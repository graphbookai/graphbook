[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "example_plugin"
version = "0.0.0"
dependencies = [
]
requires-python = ">=3.10"
authors = [
]

description = ""
